version: '3.8'
services:
  web:
    build:
      context: ./
    volumes:
      - .:/src
    command: npm run start
    ports:
      - "8080:8080"
    environment:
      DEBUG: nodejs-docker-express:*